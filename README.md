# Client Boards

Static host for the Client Board configurator.

A board is not stored here. Every board travels compressed inside the URL
fragment (everything after the `#`), which browsers never send to a server.
This repository is the empty application shell and nothing else: opening it
without a fragment shows a "this link looks incomplete" message.

Source lives elsewhere. Build output only is copied here.
